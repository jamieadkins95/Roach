<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jamieadkins.gwent.deckbuilder">

    <dist:module
        dist:instant="true"
        dist:title="@string/title_deckbuilder">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>

    <application>

        <activity
            android:name="DeckDetailsActivity"
            android:windowSoftInputMode="adjustNothing"
            android:theme="@style/Theme.Gwent.NoActionBar" />

        <activity
            android:name=".trynow.TryNowActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Gwent.NoActionBar.Full">

            <intent-filter
                android:autoVerify="true"
                tools:targetApi="m">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="gwent-9e62a.firebaseapp.com" android:path="/gwent/deck"/>
            </intent-filter>

            <meta-data
                android:name="default-url"
                android:value="https://gwent-9e62a.firebaseapp.com/gwent/deck?trynow=true" />
        </activity>

    </application>
</manifest>

